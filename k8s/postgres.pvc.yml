apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgresdbclaim
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: azurefile
  volumeName: postgrespv
  resources:
    requests:
      storage: 2Gi
  